[project]
name = "transformer-10-digit-addition"
version = "0.1.0"
description = "Low-rank tiny transformer experiments for 10-digit addition"
readme = "README.md"
requires-python = ">=3.13,<3.14"
dependencies = [
  "numpy==2.3.3",
  "torch==2.10.0",
]

[tool.uv]
package = false
